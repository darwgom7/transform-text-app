import { Routes } from '@angular/router';
import { CsvTransformerComponent } from './components/csv-transformer/csv-transformer.component';

export const csvToolsRoutes: Routes = [
  { path: '', component: CsvTransformerComponent },
];